#include<stdio.h>
main()
{
	int r,n,sum=0,temp;
	scanf("%d",n);
	temp=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(sum==temp)
	printf("armstrong number");
	else
	printf("not an armstrong number");
}
