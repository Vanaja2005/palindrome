#include<stdio.h>
int main(){
	int num,rem,rev=0,t;
	scanf("%d",num);
	t=num;
	while(num>0){
	rem=num%10;
	rev=rev+10+rem;
	num=num/10;
}
	if(t==rev)
	printf("palindrome");
	else
	printf("not a palindrome");
}
